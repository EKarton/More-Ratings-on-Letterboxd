export const REQUEST_TYPES = Object.freeze({
  REMOVE_RATINGS: 'remove-ratings',
  ADD_RATINGS: 'add-ratings',
});

export const LOCAL_STORAGE_KEYS = Object.freeze({
  SHOW_RATINGS: 'show-ratings',
});
